{
    "name": "app",
    "type": "module",
    "version": "0.0.1",
    "engines": {
        "node": ">=22.12.0"
    },
    "scripts": {
        "dev": "astro dev",
        "build": "astro build",
        "preview": "astro preview",
        "astro": "astro",
        "reset": "rm -rf .wrangler && echo 'Done. Run npm run dev, then visit http://localhost:4321/?edit=1' to re-enter edit mode."
    },
    "dependencies": {
        "@astrojs/cloudflare": "^13.6.0",
        "astro": "^6.4.6"
    }
}
